<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        /* CSS styles translated to inline styles */
        .reset_password_popup {
            position: absolute;
            z-index: 1;
            width: 100%;
            height: 100%;
            background-color: rgba(248, 226, 226, 0.449);
            display: flex;
            justify-content: center;
            align-items: start;
        }

        .reset_password_popup_container {
            width: max(23vw, 330px);
            color: black;
            background-color: #f0d7a7;
            display: flex;
            flex-direction: column;
            gap: 25px;
            margin-top: 100px;
            padding: 25px 30px;
            border-radius: 8px;
            font-size: 14px;
            animation: fadeIn 0.5s;
        }

        .reset_password_popup_title {
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: black;
        }

        .reset_password_popup_inputs {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .reset_password_popup_inputs input {
            outline: none;
            border: 1px solid rgb(0, 0, 0);
            background-color: transparent;
            padding: 8px;
            border-radius: 5px;
            color: #bd9172;
        }

        .reset_password_popup_container button {
            border: none;
            padding: 10px;
            border-radius: 4px;
            color: #000000;
            background-color: #894e3f;
            font-size: 15px;
            cursor: pointer;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="reset_password_popup">
        <div class="reset_password_popup_container">
            <h1 class="reset_password_popup_title">Reset Password</h1>
            <h1><%=email %></h1>
            <form action="" method="post" class="reset_password_popup_inputs">
                <label for="password">New Password:</label>
                <input type="password" id="password" name="password">
                <label for="confirmPassword">Confirm Password:</label>
                <input type="password" id="confirmPassword" name="confirmPassword">
                <button type="submit">Reset Password</button>
            </form>
        </div>
    </div>
</body>
</html>
